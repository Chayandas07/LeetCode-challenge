class Solution {
    public int mirrorDistance(int n) {
        return Math.abs(n - reverse(n));
    }

    public int reverse(int n){
        int t = 0;
        while(n > 0){
            t *= 10;
            t += n % 10;
            n /= 10;
        }
        return t;
    }
}
