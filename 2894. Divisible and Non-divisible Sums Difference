class Solution {
public:
    int differenceOfSums(int n, int m) {
        // we need to find S(n)-2*(divisibles)

        // no of divisibles
        int x=n/m;

        return (n*(n+1))/2-2*m*((x*(x+1))/2);
    }
};
